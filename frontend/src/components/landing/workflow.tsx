import { SectionHeading } from "./shared";
const steps = [["01", "Create", "Tender creation or upload"], ["02", "Structure", "Tender Blueprint"], ["03", "Submit", "Proposal + evidence"], ["04", "Verify", "Documents + rules"], ["05", "Evaluate", "Specialist findings"], ["06", "Decide", "Comparison + award"]];
export function Workflow() { return <section className="section workflow-section" id="workflow"><div className="container"><SectionHeading eyebrow="Procurement lifecycle" title={<>A controlled path from <span className="muted-title">intent to award.</span></>} /><div className="workflow-rail">{steps.map(([number, title, copy], index) => <div className="workflow-step" key={title}><div className="workflow-node"><span>{number}</span>{index < steps.length - 1 ? <i /> : null}</div><strong>{title}</strong><small>{copy}</small></div>)}</div><div className="workflow-status"><span><i /> Pipeline architecture</span><span>Traceability maintained across every transition</span></div></div></section>; }
