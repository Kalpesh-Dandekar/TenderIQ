import { SectionHeading } from "./shared";
const chain = [["Requirement", "Average turnover", "≥ ₹27.27L", "FIN / 04"], ["Vendor claim", "Declared turnover", "₹68.0L", "PROPOSAL / 16"], ["Supporting evidence", "Turnover_Certificate.pdf", "Page 12", "VERIFIED SOURCE"], ["Rule check", "₹68.0L ≥ ₹27.27L", "PASS", "DETERMINISTIC"]] as const;
export function EvidenceEvaluation() { return <section className="section evidence-section"><div className="container"><SectionHeading eyebrow="Evidence-grounded evaluation" title={<>Every finding has a <span className="muted-title">line of sight.</span></>} copy="Claims do not stand alone. TenderIQ is designed to connect them to requirements, supporting documents, and transparent evaluation logic." /><div className="evidence-console"><div className="console-bar"><span><i /> EVIDENCE TRACE</span><small>Evaluation ref. EV-2048</small></div><div className="evidence-chain">{chain.map(([label, title, value, meta], index) => <div className="chain-item" key={label}><div className="chain-index">{String(index + 1).padStart(2, "0")}</div><div><small>{label}</small><strong>{title}</strong><span>{meta}</span></div><b className={value === "PASS" ? "pass" : ""}>{value}</b>{index < chain.length - 1 ? <i className="chain-connector" /> : null}</div>)}</div><div className="compliance-result"><span><i /> Validation complete</span><strong>COMPLIANT</strong><small>Evidence chain intact</small></div></div></div></section>; }
